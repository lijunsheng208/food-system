# FamilyOS 开发约定

本文件适用于整个仓库。子目录中的 `AGENTS.md` 可以补充更具体的规则；发生冲突时，以距离目标文件最近的规则为准。

## 基本原则

- 修改前先阅读相关调用链、数据结构和现有测试，遵循仓库已有实现方式。
- 只修改完成当前任务所必需的代码，不顺手重构无关模块。
- 优先保持代码直接、清晰和可测试，不为了假设中的未来需求提前增加抽象。
- 不要过度防御化。已经由类型系统、框架绑定、上游校验或明确内部契约保证的数据，不要在每一层重复做空值、范围和状态检查。
- 系统边界必须正常防御：HTTP/gRPC 输入、数据库和 Redis 返回值、文件、环境变量、第三方 API、JWT 与用户权限都必须校验，并正确处理错误。
- 遇到违反内部不变量的情况，优先明确返回错误或快速失败，不要静默吞掉错误、伪造默认值或让程序带着错误状态继续运行。

## 中文注释

- 每个手写的命名函数和方法前都必须添加中文注释，说明函数的用途、关键输入输出或业务责任。
- Go 导出函数的注释应以函数名开头，满足 GoDoc 和静态检查要求。
- TypeScript/React 中的组件、Hook、服务函数和具名辅助函数前必须添加中文注释。
- 匿名回调不要求逐个添加注释；当匿名回调承载关键业务流程时，应在代码块前添加中文注释。
- 关键业务逻辑必须添加中文注释，包括权限判断、状态流转、事务边界、幂等处理、Token 轮换、并发控制、限流、缓存一致性和异常回退。
- 注释重点解释“为什么这样处理”和业务约束，不要只把代码逐字翻译成中文。
- 修改代码后同步更新已经失真的注释，不保留与行为不一致的旧注释。
- Proto 生成文件、第三方代码和工具自动生成文件不手工补注释；应修改源文件后重新生成。

示例：

```go
// RefreshToken 轮换当前会话的刷新令牌，并使旧令牌立即失效。
func (s *AuthService) RefreshToken(ctx context.Context, token string) (*TokenPair, error) {
    // 轮换必须在 Redis 中原子完成，避免同一个旧令牌被并发使用两次。
    return s.sessions.Rotate(ctx, token)
}
```

```ts
// refreshAccessToken 使用刷新令牌换取新凭证，并保证并发请求共享同一次刷新操作。
async function refreshAccessToken(): Promise<string> {
  // 多个请求同时收到 401 时只允许发起一次刷新，其余请求等待同一个 Promise。
  return getOrCreateRefreshPromise();
}
```

## 错误处理

- 错误信息应提供必要上下文，但不得包含密码、验证码、完整 Token、数据库连接串或第三方密钥。
- 不要使用空的 `catch`、无条件忽略错误或仅记录日志后继续返回成功。
- 不要为每次函数调用增加没有恢复策略的 `try/catch`；能由统一错误处理中间件处理的错误交给统一层处理。
- HTTP 和 gRPC 状态码应与业务错误码含义一致，不用 HTTP 200 包装所有失败。
- 只有在业务上确实允许降级时才提供默认值，并用中文注释说明降级条件。

## 后端约定

- Handler 负责协议解析、身份提取和响应映射；业务规则放在 Service；持久化细节放在 Repository。
- 用户身份必须来自经过验证的 Access Token，不接受客户端提交的 `user_id` 作为当前操作者身份。
- 数据库多步写操作涉及一致性时使用事务，并在关键位置说明事务边界。
- Redis 中的验证码、刷新令牌等敏感值只保存摘要；日志中不得输出完整值。
- 修改 `.proto` 后重新生成对应文件，不直接编辑 `proto/gen` 下的生成代码。
- 新增配置项时同步维护 `config.yaml.example`，真实 `config.yaml` 和真实凭证不得提交。

## 移动端约定

- API 请求统一通过现有服务层，不在页面组件中重复创建网络客户端或认证逻辑。
- Access Token、Refresh Token 等敏感凭证使用安全存储，不写入普通持久化存储或日志。
- 页面组件负责交互和展示，复杂业务状态提取到服务、Hook 或上下文中。
- 异步操作必须覆盖加载、成功和失败状态，防止重复提交；不要为不可能发生的状态堆叠无意义判断。
- 导航参数和 API 类型应复用已有类型定义，避免使用 `any` 绕过类型检查。

## 测试与验证

- 修改核心业务逻辑时补充或更新测试，优先覆盖正常流程、明确的业务失败和关键边界。
- 修复缺陷时尽量添加能够复现该缺陷的回归测试。
- 测试应验证外部行为，不要过度绑定内部实现细节。
- 提交前运行受影响模块的格式化、静态检查和测试；无法运行时明确说明原因。
- 不为了让测试通过而降低生产代码的校验、权限或安全要求。

## Git 与文件范围

- 不提交真实 `config.yaml`、`.env`、密钥、数据库密码、本机 IP、构建产物或依赖目录。
- 不修改或撤销与当前任务无关的本地改动。
- 一个提交聚焦一个完整主题，生成文件与其源文件放在同一个提交中。
- 提交信息使用清晰的 Conventional Commits 格式，例如 `feat(auth): 实现双 Token 会话`。
