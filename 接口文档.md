# FamilyOS 接口文档

## 概述

- **网关地址**：`http://{host}:8080`（开发环境 `192.168.1.5:8080`）
- **请求格式**：JSON
- **字符编码**：UTF-8
- **认证方式**：JWT Bearer Token（登录/注册成功后返回，有效期 7 天）

---

## 业务状态码

| 状态码 | 含义 |
|--------|------|
| `0` | 成功 |
| `1001` | 手机号格式不正确（需满足 `1[3-9]xxxxxxxxx`） |
| `1002` | 密码长度不能少于 6 位 |
| `1003` | 该手机号已注册 |
| `1004` | 用户不存在 |
| `1005` | 密码错误 |
| `1006` | 账号已被禁用 |
| `1999` | 服务内部错误 |

所有响应均包含 `code` 和 `message` 字段。`code = 0` 表示成功，`code ≠ 0` 表示失败。

---

## 1. 健康检查

### GET /health

**请求参数**：无

**成功响应** (HTTP 200)：

```json
{
  "status": "ok"
}
```

---

## 2. 用户注册

### POST /api/v1/auth/register

**请求体** (JSON)：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `phone` | string | 是 | 手机号，格式 `1[3-9]xxxxxxxxx` |
| `password` | string | 是 | 密码，最少 6 位 |
| `nickname` | string | 是 | 昵称 |

**请求示例**：

```json
{
  "phone": "13800138000",
  "password": "123456",
  "nickname": "小明"
}
```

**成功响应** (HTTP 200)：

```json
{
  "code": 0,
  "message": "注册成功",
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user_id": 1
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `code` | int | 0 表示成功 |
| `message` | string | 提示信息 |
| `token` | string | JWT Token，有效期 7 天 |
| `user_id` | int | 新创建的用户 ID |

**失败响应示例**：

```json
{
  "code": 1003,
  "message": "手机号已注册",
  "token": "",
  "user_id": 0
}
```

**校验规则**：
- 手机号正则：`^1[3-9]\d{9}$`
- 密码长度 ≥ 6
- 手机号唯一，重复注册返回 `1003`

---

## 3. 用户登录

### POST /api/v1/auth/login

**请求体** (JSON)：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `phone` | string | 是 | 手机号 |
| `password` | string | 是 | 密码 |

**请求示例**：

```json
{
  "phone": "13800138000",
  "password": "123456"
}
```

**成功响应** (HTTP 200)：

```json
{
  "code": 0,
  "message": "登录成功",
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": 1,
    "phone": "13800138000",
    "nickname": "小明",
    "avatar": "",
    "gender": 0,
    "status": 1,
    "last_login_at": "2026-06-27 20:30:15",
    "created_at": "2026-06-27 19:00:00",
    "updated_at": "2026-06-27 20:30:15"
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `code` | int | 0 表示成功 |
| `message` | string | 提示信息 |
| `token` | string | JWT Token，有效期 7 天 |
| `user` | object | 用户信息对象 |
| `user.id` | int | 用户 ID |
| `user.phone` | string | 手机号 |
| `user.nickname` | string | 昵称 |
| `user.avatar` | string | 头像 URL（空字符串表示未设置） |
| `user.gender` | int | `0` 未知 `1` 男 `2` 女 |
| `user.status` | int | `1` 正常 `2` 已禁用 |
| `user.last_login_at` | string | 最后登录时间 `YYYY-MM-DD HH:mm:ss` |
| `user.created_at` | string | 注册时间 |
| `user.updated_at` | string | 更新时间 |

**失败响应示例**：

```json
{
  "code": 1004,
  "message": "用户不存在",
  "token": "",
  "user": null
}
```

**登录失败场景**：
- `1004` — 手机号未注册
- `1005` — 密码不匹配
- `1006` — 账号已被禁用

---

## 架构

```
客户端 (移动端 / curl / Postman)
  │
  │  HTTP JSON  (port 8080)
  ▼
Gateway Service (Gin)
  │
  │  gRPC  (port 50051)
  ▼
Logic Service (gRPC)
  │
  ▼
MySQL (familyos_logic)
```

客户端直接调用 Gateway 的 HTTP 接口，Gateway 代理转发到 gRPC 后端。
