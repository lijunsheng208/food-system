# FamilyOS 接口文档

## 概述

- **网关地址**：`http://{host}:8080`（开发环境 `192.168.1.5:8080`）
- **请求格式**：JSON
- **字符编码**：UTF-8
- **认证方式**：15 分钟 Access JWT + 30 天 Redis Refresh Session

---

## 业务状态码

| 状态码 | 含义 |
|--------|------|
| `0` | 成功 |
| `1001` | 手机号格式不正确（需满足 `1[3-9]xxxxxxxxx`） |
| `1002` | 密码长度不能少于 6 位 |
| `1003` | 该手机号已注册 |
| `1004` | 用户不存在 |
| `1005` | 密码错误 |
| `1006` | 账号已被禁用 |
| `1007` | 验证码发送过于频繁 |
| `1008` | 该手机号今日发送次数已达上限 |
| `1009` | 当前网络发送次数过多 |
| `1010` | 短信发送失败 |
| `1011` | 短信验证码服务暂不可用 |
| `1012` | 验证码无效或已过期 |
| `1013` | 验证码错误次数过多 |
| `1014` | 账号尚未设置密码，请使用验证码登录 |
| `1015` | Refresh Token 无效或已过期 |
| `1016` | Refresh Token 已失效或被重复使用 |
| `1017` | 登录会话服务暂不可用 |
| `1018` | Access Token 无效或已过期 |
| `1999` | 服务内部错误 |

所有响应均包含 `code` 和 `message` 字段。`code = 0` 表示成功，`code ≠ 0` 表示失败。

---

## 短信验证码（第一阶段）

### POST /api/v1/auth/sms/code

第一阶段使用控制台短信发送器。验证码只出现在 Logic Service 开发日志中，Redis 保存验证码的 HMAC 摘要，不保存明文。

**请求体**：

```json
{
  "phone": "13800138000"
}
```

客户端 IP 由 Gateway 从 HTTP 请求中提取，客户端不能自行提交。验证码有效期 5 分钟，同一手机号 60 秒内不能重复发送。

**成功响应**（HTTP 200）：

```json
{
  "code": 0,
  "message": "验证码已发送",
  "retry_after_seconds": 60
}
```

频率限制返回 HTTP 429，并通过 `retry_after_seconds` 告知可重试时间。短信或 Redis 不可用返回 HTTP 503。

启动 Logic Service 前必须注入独立摘要密钥：

```bash
export FAMILYOS_SMS_CODE_HMAC_SECRET="replace-with-a-long-random-secret"
```

### POST /api/v1/auth/sms/login

使用六位短信验证码登录。手机号尚未注册时会自动创建用户，默认昵称为“用户 + 手机号后四位”。验证码成功后立即失效，最多允许连续输错五次。

**请求体**：

```json
{
  "phone": "13800138000",
  "verification_code": "483921",
  "device_id": "device-stable-id"
}
```

`device_id` 用于标记当前 Refresh Session 对应的客户端设备。

**成功响应**（HTTP 200）：

```json
{
  "code": 0,
  "message": "登录成功",
  "access_token": "15分钟Access JWT",
  "refresh_token": "session-id.refresh-secret",
  "expires_in": 900,
  "is_new_user": true,
  "user": {
    "id": 1001,
    "phone": "13800138000",
    "nickname": "用户8000",
    "avatar": "",
    "gender": 0,
    "status": 1,
    "last_login_at": "2026-08-09 12:00:00",
    "created_at": "2026-08-09 12:00:00",
    "updated_at": "2026-08-09 12:00:00"
  }
}
```

无效或过期验证码返回 HTTP 400 / `1012`，第五次错误及锁定期间返回 HTTP 429 / `1013`，Redis 不可用返回 HTTP 503 / `1011`。

启用自动注册前需执行：

```text
sql/20260809_make_user_password_nullable.sql
```

## Token 和 Session

所有注册和登录接口都会返回：

```json
{
  "access_token": "短期 JWT",
  "refresh_token": "仅客户端持有的随机 Token",
  "expires_in": 900
}
```

Access JWT 包含 `sub`、`sid`、`jti`、`iss`、`aud`、`iat` 和 `exp`，有效期默认 15 分钟。Refresh Session 在 Redis 保存 30 天，Redis 只保存 Refresh Secret 的 SHA-256 摘要，不保存完整 Refresh Token。

### POST /api/v1/auth/refresh

Access Token 过期前或过期后，使用 Refresh Token 换取一套新 Token。每次刷新都会原子替换 Redis 中的 Secret 摘要并重置 30 天 TTL，旧 Refresh Token 再次使用返回 `1016`。

```json
{
  "refresh_token": "session-id.refresh-secret"
}
```

成功响应：

```json
{
  "code": 0,
  "message": "Token 刷新成功",
  "access_token": "新的Access JWT",
  "refresh_token": "Rotation后的新Refresh Token",
  "expires_in": 900
}
```

客户端必须以原子方式覆盖本地保存的 Access Token 和 Refresh Token。不得在刷新成功后继续使用旧 Refresh Token。

移动端分别在 SecureStore 保存 `auth_access_token`、`auth_refresh_token` 和 `auth_access_token_expires_at`。业务请求遇到 HTTP 401 时，全局只创建一个刷新 Promise；同期失败请求等待该 Promise，刷新成功后各自重放一次。刷新接口使用独立 Axios 实例，不进入业务响应拦截器。刷新失败时清空三项凭证并重置导航到登录页。

### POST /api/v1/auth/logout

注销当前设备对应的 Refresh Session。可通过 JSON 请求体提交 Refresh Token，也可以使用 `Authorization` Header：

```http
Authorization: Bearer <refresh_token>
```

成功响应：

```json
{
  "code": 0,
  "message": "退出登录成功"
}
```

退出后，已经签发的 Access JWT 最多继续有效 15 分钟；Refresh Token 立即失效。

## 认证中间件

个人资料、头像上传、家庭和菜单接口必须携带 Access Token：

```http
Authorization: Bearer <access_token>
```

Gateway 校验 HS256 算法、签名、必填的 `exp`、`iss` 和 `aud`，并从 `sub` 获取当前用户 ID；缺少 `sid` 或 `jti` 的旧式 JWT 同样会被拒绝。客户端提交的 `user_id`、`created_by` 或 `operator_user_id` 不再作为身份依据；业务目标字段如 `member_user_id`、`cook_user_id` 仍然保留。校验失败返回 HTTP 401 / `1018`。

---

## 1. 健康检查

### GET /health

**请求参数**：无

**成功响应** (HTTP 200)：

```json
{
  "status": "ok"
}
```

---

## 2. 用户注册

### POST /api/v1/auth/register

**请求体** (JSON)：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `phone` | string | 是 | 手机号，格式 `1[3-9]xxxxxxxxx` |
| `password` | string | 是 | 密码，最少 6 位 |
| `nickname` | string | 是 | 昵称 |
| `device_id` | string | 否 | 设备稳定标识 |

**请求示例**：

```json
{
  "phone": "13800138000",
  "password": "123456",
  "nickname": "小明",
  "device_id": "device-stable-id"
}
```

**成功响应** (HTTP 200)：

```json
{
  "code": 0,
  "message": "注册成功",
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "refresh_token": "session-id.refresh-secret",
  "expires_in": 900,
  "user_id": 1,
  "user": {
    "id": 1,
    "phone": "13800138000",
    "nickname": "小明",
    "status": 1
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `code` | int | 0 表示成功 |
| `message` | string | 提示信息 |
| `user_id` | int | 新创建的用户 ID |
| `user` | object | 新创建的用户信息 |

**失败响应示例**：

```json
{
  "code": 1003,
  "message": "手机号已注册",
  "user_id": 0
}
```

**校验规则**：
- 手机号正则：`^1[3-9]\d{9}$`
- 密码长度 ≥ 6
- 手机号唯一，重复注册返回 `1003`

---

## 3. 用户登录

### POST /api/v1/auth/login

**请求体** (JSON)：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `phone` | string | 是 | 手机号 |
| `password` | string | 是 | 密码 |
| `device_id` | string | 否 | 设备稳定标识 |

**请求示例**：

```json
{
  "phone": "13800138000",
  "password": "123456",
  "device_id": "device-stable-id"
}
```

**成功响应** (HTTP 200)：

```json
{
  "code": 0,
  "message": "登录成功",
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "refresh_token": "session-id.refresh-secret",
  "expires_in": 900,
  "user": {
    "id": 1,
    "phone": "13800138000",
    "nickname": "小明",
    "avatar": "",
    "gender": 0,
    "status": 1,
    "last_login_at": "2026-06-27 20:30:15",
    "created_at": "2026-06-27 19:00:00",
    "updated_at": "2026-06-27 20:30:15"
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `code` | int | 0 表示成功 |
| `message` | string | 提示信息 |
| `user` | object | 用户信息对象 |
| `user.id` | int | 用户 ID |
| `user.phone` | string | 手机号 |
| `user.nickname` | string | 昵称 |
| `user.avatar` | string | 头像 URL（空字符串表示未设置） |
| `user.gender` | int | `0` 未知 `1` 男 `2` 女 |
| `user.status` | int | `1` 正常 `2` 已禁用 |
| `user.last_login_at` | string | 最后登录时间 `YYYY-MM-DD HH:mm:ss` |
| `user.created_at` | string | 注册时间 |
| `user.updated_at` | string | 更新时间 |

**失败响应示例**：

```json
{
  "code": 1004,
  "message": "用户不存在",
  "user": null
}
```

**登录失败场景**：
- `1004` — 手机号未注册
- `1005` — 密码不匹配
- `1006` — 账号已被禁用

---

## 4. 查询个人信息

### GET /api/v1/auth/profile

查询用户的个人信息（头像、昵称、手机号、性别、家庭等）。

**请求示例**：

```
GET /api/v1/auth/profile
```

**成功响应** (HTTP 200)：

```json
{
  "code": 0,
  "message": "查询成功",
  "user": {
    "id": 1,
    "phone": "13800138000",
    "nickname": "小明",
    "avatar": "https://bucket.oss-cn-beijing.aliyuncs.com/avatars/user_1_20260705.jpg",
    "gender": 1,
    "status": 1,
    "last_login_at": "2026-07-05 02:30:00",
    "created_at": "2026-06-27 19:00:00",
    "updated_at": "2026-07-05 02:40:00"
  },
  "family_name": ""
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `user` | object | 用户信息（字段同登录接口返回的 user） |
| `family_name` | string | 当前所在家庭名称，空字符串表示未加入家庭 |

**失败响应示例**：

```json
{ "code": 1004, "message": "用户不存在" }
```

---

## 5. 更新个人信息

### PUT /api/v1/auth/profile

更新用户昵称、头像、性别等个人信息，支持**增量更新**（只传需要修改的字段）。

**请求体** (JSON)：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `nickname` | string | 否 | 新昵称，空字符串表示不修改 |
| `avatar` | string | 否 | 头像 URL，空字符串表示不修改 |
| `gender` | int | 否 | `-1` 不修改，`0` 未知，`1` 男，`2` 女 |

**请求示例**：

```json
{
  "nickname": "大明",
  "gender": 1
}
```

**成功响应** (HTTP 200)：

```json
{
  "code": 0,
  "message": "更新成功"
}
```

**增量更新说明**：
- 只传 `nickname` → 只改昵称，头像和性别不变
- 只传 `gender: 1` → 只改性別
- 只传 `avatar: "https://..."` → 只改头像

---

## 6. 上传头像

### POST /api/v1/upload/avatar

上传用户头像图片到 OSS，返回可访问的 URL。拿到 URL 后需调 `PUT /api/v1/auth/profile` 写入数据库。

**请求体** (multipart/form-data)：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `file` | file | 是 | 图片文件，≤ 5MB |

**请求示例**：

```bash
curl -X POST http://192.168.1.5:8080/api/v1/upload/avatar \
  -H "Authorization: Bearer <access_token>" \
  -F "file=@/path/to/avatar.jpg"
```

**成功响应** (HTTP 200)：

```json
{
  "code": 0,
  "message": "上传成功",
  "url": "https://public-food-storage.oss-cn-beijing.aliyuncs.com/avatars/user_1_20260705143025.jpg"
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `url` | string | OSS 公网可访问的图片 URL |

**限制**：
- 文件大小 ≤ 5MB
- 支持格式：jpg, jpeg, png, gif, webp, bmp
- OSS 存储路径：`avatars/user_{id}_{timestamp}.{ext}`

---

## 7. 查询菜谱分类

### GET /api/v1/dish/categories

获取所有启用的菜谱分类，按排序升序返回。

**请求参数**：无

**请求示例**：

```
GET /api/v1/dish/categories
```

**成功响应** (HTTP 200)：

```json
{
  "code": 0,
  "message": "查询成功",
  "categories": [
    { "id": 1, "name": "家常菜", "sort": 1 },
    { "id": 2, "name": "汤羹", "sort": 2 },
    { "id": 3, "name": "主食", "sort": 3 },
    { "id": 4, "name": "凉菜", "sort": 4 },
    { "id": 5, "name": "甜品", "sort": 5 }
  ]
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `categories` | array | 分类列表 |
| `categories[].id` | int | 分类 ID |
| `categories[].name` | string | 分类名称 |
| `categories[].sort` | int | 排序值（越小越靠前） |

---

## 8. 查询分类下菜谱

### GET /api/v1/dish/dishes

根据分类 ID 查询该分类下所有上架菜谱。

**请求参数** (Query)：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `category_id` | int | 是 | 分类 ID |

**请求示例**：

```
GET /api/v1/dish/dishes?category_id=1
```

**成功响应** (HTTP 200)：

```json
{
  "code": 0,
  "message": "查询成功",
  "dishes": [
    {
      "id": 1,
      "category_id": 1,
      "name": "红烧肉",
      "description": "肥而不腻，入口即化的经典家常硬菜",
      "image_key": "",
      "sort": 1,
      "created_at": "2026-06-29 12:00:00",
      "updated_at": "2026-06-29 12:00:00"
    },
    {
      "id": 2,
      "category_id": 1,
      "name": "鱼香肉丝",
      "description": "酸甜微辣，下饭神菜",
      "image_key": "",
      "sort": 2,
      "created_at": "2026-06-29 12:00:00",
      "updated_at": "2026-06-29 12:00:00"
    }
  ]
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `dishes` | array | 菜谱列表 |
| `dishes[].id` | int | 菜谱 ID |
| `dishes[].category_id` | int | 所属分类 ID |
| `dishes[].name` | string | 菜名 |
| `dishes[].description` | string | 描述 |
| `dishes[].image_key` | string | OSS 图片 key（空字符串表示暂无图片） |
| `dishes[].sort` | int | 排序值 |
| `dishes[].created_at` | string | 创建时间 |
| `dishes[].updated_at` | string | 更新时间 |

---

## 9. 搜索菜谱

### GET /api/v1/dish/search

按菜名关键字模糊搜索已上架的菜谱。

**请求参数** (Query)：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `keyword` | string | 是 | 搜索关键字，模糊匹配菜名 |

**请求示例**：

```
GET /api/v1/dish/search?keyword=红烧
```

**成功响应** (HTTP 200)：

```json
{
  "code": 0,
  "message": "搜索成功",
  "dishes": [
    {
      "id": 1,
      "category_id": 1,
      "name": "红烧肉",
      "description": "肥而不腻，入口即化的经典家常硬菜",
      "image_key": "",
      "sort": 1,
      "created_at": "2026-06-29 12:00:00",
      "updated_at": "2026-06-29 12:00:00"
    },
    {
      "id": 7,
      "category_id": 1,
      "name": "红烧排骨",
      "description": "",
      "image_key": "",
      "sort": 7,
      "created_at": "2026-06-29 12:00:00",
      "updated_at": "2026-06-29 12:00:00"
    }
  ]
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `dishes` | array | 匹配的菜谱列表（字段同上） |

**说明**：
- 搜索范围仅限状态为"上架"的菜谱
- 使用 SQL `LIKE %keyword%` 模糊匹配菜名

---

## 10. 查询菜谱详情

### GET /api/v1/dish/:id

根据菜谱 ID 查询已上架菜谱的基本信息、食材分组和制作步骤。

**路径参数**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | int | 是 | 菜谱 ID |

**请求示例**：

```
GET /api/v1/dish/1
```

**成功响应** (HTTP 200)：

```json
{
  "code": 0,
  "message": "查询成功",
  "dish": {
    "id": 1,
    "category_id": 1,
    "name": "红烧肉",
    "description": "肥而不腻的经典家常菜",
    "image_key": "dishes/1/cover.jpg",
    "cook_minutes": 45,
    "difficulty": 2,
    "servings": 3,
    "tips": "炒糖色时使用小火。",
    "ingredient_groups": [
      {
        "name": "主料",
        "ingredients": [
          {
            "id": 1,
            "name": "五花肉",
            "amount": 500,
            "amount_text": "",
            "unit": "克",
            "sort": 1
          }
        ]
      }
    ],
    "steps": [
      {
        "id": 1,
        "step_no": 1,
        "description": "五花肉切块，冷水下锅焯水。",
        "image_key": "",
        "timer_seconds": 0
      }
    ]
  }
}
```

`amount` 为 `null` 时使用 `amount_text` 展示“适量”“少许”等文字用量。食材分组按 `group_sort` 排序，组内食材按 `sort` 排序，步骤按 `step_no` 排序。`timer_seconds` 为步骤倒计时秒数，值为 `0` 表示该步骤不需要计时。

**失败响应**：

```json
{ "code": 2001, "message": "菜谱ID不正确" }
```

```json
{ "code": 2002, "message": "菜谱不存在" }
```

---

## 11. 加入家庭菜单

### POST /api/v1/family/meal-plans

将一道已上架菜谱加入指定家庭的用餐计划。创建人必须是该家庭成员；`cook_user_id` 可不传，传入时也必须是该家庭成员。

**请求体**：

```json
{
  "family_id": 1,
  "dish_id": 12,
  "meal_date": "2026-08-06",
  "meal_type": 3,
  "servings": 4,
  "cook_user_id": 8
}
```

`meal_type`：`1` 早餐、`2` 午餐、`3` 晚餐。`servings` 取值范围为 1 到 20。

**成功响应** (HTTP 201)：

```json
{
  "code": 0,
  "message": "已加入家庭菜单",
  "meal_plan": {
    "id": 31,
    "family_id": 1,
    "dish_id": 12,
    "meal_date": "2026-08-06",
    "meal_type": 3,
    "servings": 4,
    "cook_user_id": 8,
    "status": 0,
    "created_by": 7,
    "created_at": "2026-08-05 20:30:00",
    "updated_at": "2026-08-05 20:30:00"
  }
}
```

**业务错误码**：

| 错误码 | 说明 |
|--------|------|
| `2001` | 家庭不存在 |
| `2004` | 创建人不是该家庭成员 |
| `2101` | 用餐日期格式不正确 |
| `2102` | 餐次类型不正确 |
| `2103` | 用餐人数不正确 |
| `2104` | 菜谱不存在或已下架 |
| `2105` | 做饭负责人不是该家庭成员 |
| `2106` | 请求中的 ID 不正确 |

---

## 12. 查询家庭菜单

### GET /api/v1/family/:family_id/meal-plans

查询指定日期范围内的家庭菜单。查询人必须是该家庭成员，日期范围最长 32 天。

```text
GET /api/v1/family/1/meal-plans?start_date=2026-08-03&end_date=2026-08-09
```

成功响应中的 `meal_plans` 按日期、餐次排序，每项除菜单字段外还包含 `dish_name`、`dish_image_key` 和 `cook_user_name`。

---

## 13. 修改家庭菜单

### PATCH /api/v1/family/meal-plans/:id

请求体中的菜单字段均为可选；未传字段保持不变。`cook_user_id` 传 `0` 表示清空负责人。

```json
{
  "meal_date": "2026-08-07",
  "meal_type": 2,
  "servings": 3,
  "cook_user_id": 0
}
```

---

## 14. 删除家庭菜单

### DELETE /api/v1/family/meal-plans/:id

```text
DELETE /api/v1/family/meal-plans/31
```

查询、修改和删除接口新增错误码：`2107` 表示菜单记录不存在，`2108` 表示查询日期范围不正确。

---

## 接口总览

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/health` | 健康检查 |
| `POST` | `/api/v1/auth/register` | 用户注册 |
| `POST` | `/api/v1/auth/login` | 用户登录 |
| `POST` | `/api/v1/auth/sms/code` | 发送短信登录验证码 |
| `POST` | `/api/v1/auth/sms/login` | 短信验证码登录/自动注册 |
| `POST` | `/api/v1/auth/refresh` | Rotation 刷新 Access/Refresh Token |
| `POST` | `/api/v1/auth/logout` | 注销当前 Refresh Session |
| `GET` | `/api/v1/auth/profile` | 查询个人信息 |
| `PUT` | `/api/v1/auth/profile` | 更新个人信息（增量更新） |
| `POST` | `/api/v1/upload/avatar` | 上传头像 |
| `GET` | `/api/v1/dish/categories` | 查询菜谱分类 |
| `GET` | `/api/v1/dish/dishes` | 查询分类下菜谱 |
| `GET` | `/api/v1/dish/search` | 搜索菜谱 |
| `GET` | `/api/v1/dish/:id` | 查询菜谱详情 |
| `POST` | `/api/v1/family/meal-plans` | 将菜谱加入家庭菜单 |
| `GET` | `/api/v1/family/:family_id/meal-plans` | 查询家庭菜单 |
| `PATCH` | `/api/v1/family/meal-plans/:id` | 修改家庭菜单 |
| `DELETE` | `/api/v1/family/meal-plans/:id` | 删除家庭菜单 |

---

## 架构

```
客户端 (移动端 / curl / Postman)
  │
  │  HTTP JSON  (port 8080)
  ▼
Gateway Service (Gin)
  │
  │  gRPC  (port 50051)
  ▼
Logic Service (gRPC)
  ├── MySQL (永久用户数据)
  └── Redis (验证码和 Refresh Session)
```

客户端直接调用 Gateway 的 HTTP 接口，Gateway 代理转发到 gRPC 后端。
